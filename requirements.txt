# --- MCP server (Claude Desktop / ChatGPT Desktop) ---
# This is all you need for the MCP server; the agent/LLM packages below are
# only used by the legacy interactive CLI (python main.py).
mcp>=1.2.0

# --- Agent framework (CLI only) ---
langchain
langchain-core
langgraph
langgraph-supervisor

# --- LLM providers ---
langchain-nvidia-ai-endpoints
langchain-groq
langchain-ollama
langchain-openai  # OpenRouter (OpenAI-compatible) endpoint

# --- Groww broker integration ---
growwapi>=1.5.0
pyotp

# --- Market data ---
yfinance
# yfinance needs >=13 (websockets.asyncio); langgraph-sdk needs <17.
websockets>=14,<17

# --- Technical + sentiment analysis ---
numpy
pandas
requests
beautifulsoup4
textblob

# --- Terminal UI ---
rich

# --- Config ---
python-dotenv

# --- Tests (dev) ---
pytest
